plugins {
    id 'java'
}

// Common sources are compiled as part of :fabric and :neoforge via srcDirs inclusion.
// Disable standalone Java compilation to avoid missing MC/Mixin classpath issues.
tasks.named('compileJava') { enabled = false }
tasks.named('classes') { enabled = false }
